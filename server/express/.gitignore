# =============================================================================
# Express.js Backend - Specific Ignores
# =============================================================================
# Common patterns (node_modules, .env, .DS_Store, etc.) are in root .gitignore
# This file only contains Express/TypeScript-specific patterns
# =============================================================================

# Build Output
dist/
build/

# TypeScript
*.tsbuildinfo

# Package Lock (optional - remove this line if you want to commit package-lock.json)
# package-lock.json
